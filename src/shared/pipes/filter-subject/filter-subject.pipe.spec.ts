import { FilterSubjectPipe } from './filter-subject.pipe';

describe('FilterSubjectPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubjectPipe();
    expect(pipe).toBeTruthy();
  });
});
